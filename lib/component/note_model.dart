class Note {
  String id;
  String subtitle;
  String title;
  String time;
  int image;
  bool isDone;
  Note(this.id, this.subtitle, this.time, this.image, this.title, this.isDone);
}//call note and sent parameter such as id, subtitle, title etc.